import { Abnoclass, AbnoData } from './abnoclass';

describe('Abnoclass', () => {
  it('should create an instance', () => {
    expect(new Abnoclass()).toBeTruthy();
  });
});

describe('AbnoData', () => {
  it('should create an instance', () => {
    expect(new AbnoData()).toBeTruthy();
  });
});
